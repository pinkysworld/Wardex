name: Site Quality

on:
  push:
    branches: [main]
    paths:
      - 'site/**'
      - 'CHANGELOG.md'
      - 'scripts/build_changelog.py'
      - '.github/workflows/site-quality.yml'
  pull_request:
    paths:
      - 'site/**'
      - 'CHANGELOG.md'
      - 'scripts/build_changelog.py'
      - '.github/workflows/site-quality.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: site-quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  a11y-and-lighthouse:
    name: Accessibility & Lighthouse
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: '20.19.0'

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: '3.12'

      - name: Install tooling
        run: |
          npm install --ignore-scripts --no-save --no-audit --no-fund \
            pa11y-ci@3.1.0 \
            @lhci/cli@0.14.0 \
            http-server@14.1.1

      - name: Provision Chromium for pa11y
        # `--ignore-scripts` above skips puppeteer's postinstall browser
        # download; rebuild it explicitly so pa11y can launch a browser.
        run: npm rebuild puppeteer

      - name: Build site preview (incl. changelog.html)
        run: |
          rm -rf site-build
          mkdir -p site-build
          cp -R site/. site-build/
          python3 scripts/build_changelog.py CHANGELOG.md site-build/changelog.html

      - name: Start static server
        run: |
          npx --yes http-server site-build -p 8765 -s --silent &
          echo $! > server.pid
          # Wait for the server to respond.
          for i in $(seq 1 20); do
            if curl -sf http://localhost:8765/index.html > /dev/null; then
              break
            fi
            sleep 0.5
          done
          curl -sI http://localhost:8765/index.html | head -1

      - name: Run pa11y-ci (WCAG 2.1 AA)
        run: |
          npx pa11y-ci \
            --config .github/pa11yci.json \
            --json > pa11y-report.json || {
              status=$?
              echo "pa11y-ci exited with status $status"
              cat pa11y-report.json || true
              exit $status
            }
        continue-on-error: false

      - name: Run Lighthouse CI
        run: npx lhci autorun --config=.github/lighthouserc.json
        env:
          LHCI_BUILD_CONTEXT__CURRENT_HASH: ${{ github.sha }}

      - name: Upload reports
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: site-quality-reports
          path: |
            pa11y-report.json
            .lighthouseci/
          if-no-files-found: ignore
          retention-days: 14

      - name: Stop static server
        if: always()
        run: |
          if [ -f server.pid ]; then
            kill "$(cat server.pid)" 2>/dev/null || true
          fi
