apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wardex-data
  labels:
    app.kubernetes.io/name: wardex
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
