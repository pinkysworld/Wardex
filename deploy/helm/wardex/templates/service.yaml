apiVersion: v1
kind: Service
metadata:
  name: {{ include "wardex.fullname" . }}
  labels:
    {{- include "wardex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "wardex.selectorLabels" . | nindent 4 }}
